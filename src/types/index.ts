/**
 * Alien UI — Types barrel export.
 * Re-exports all shared types for consumers and internal use.
 */

export type {
  AlienSize,
  AlienFieldState,
  AlienColorMode,
  AlienBaseProps,
  AlienFormFieldProps,
  RtlLocale,
} from './global'

export { RTL_LOCALES } from './global'

export type { AlienThemeTokens, AlienThemeConfig } from './theme'

export type { AlienMessages } from './i18n'
