/**
 * Alien UI — Variants sub-entry.
 *
 * Import path: `alien-ui/variants`
 *
 * Use this when you only want to import variant functions (cva definitions)
 * without importing Vue components — useful for headless usage or
 * when extending variants in your own project.
 *
 * @example
 * import { inputVariants } from '@alien-ui/vue/variants'
 */

export {
  inputWrapperVariants,
  inputFieldVariants,
  inputClearVariants,
  inputLabelVariants,
  inputHintVariants,
} from './components/forms/Input/Input.variants'

// export { buttonVariants }  from './components/forms/Button/Button.variants'
// export { badgeVariants }   from './components/feedback/Badge/Badge.variants'
